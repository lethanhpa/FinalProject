import React, {memo} from "react"

function Account(){
    return(
        <div className="container my-[50px] flex justify-center">
            Account nè!
        </div>
    )
}

export default memo(Account);